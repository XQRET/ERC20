# this is our wallet

> our first edition of purse in testing...coming soon.

## 1.0.0 will coming

### RETToken

> We think that it's important for use different language 

```
token总量，默认会为public变量生成一个getter函数接口，名称为totalSupply().
Token amount, will default to public variables generated a getter function interface, called totalSupply ().

获取账户_owner拥有token的数量
The number of access account _owner has token

从消息发送者账户中往_to账户转数量为_value的token
From the message sender account to turn number for the token _value _to account

从账户_from中往账户_to转数量为_value的token，与approve方法配合使用
Turn into account from the account _from _to number for _value token, used with the approve method

消息发送账户设置账户_spender能从发送账户中转出数量为_value的token
Messages account set up account _spender can transit from sending account number for _value token

获取账户_spender可以从账户_owner中转出token的数量
Can get account _spender _owner transfer from account the number of the token

发生转账时必须要触发的事件
Must want to trigger events when transfer occurs

当函数approve(address _spender, uint256 _value)成功执行时必须触发的事件
When the function the approve (address _spender, uint256 _value) must trigger events when executed successfully

默认totalSupply 不会超过最大值 (2^256 - 1)
The default totalSupply will not exceed the maximum (2 ^ 256-1)

如果随着时间的推移将会有新的token生成，则可以用下面这句避免溢出的异常
If over time there will be a new token generated, you can use the following to avoid overflow exception

从消息发送者账户中减去token数量_value
From the message sender account number minus token _value

往接收账户增加token数量_value
To increase the number of token _value receiving account

触发转币交易事件
Turn currency trading event trigger

接收账户增加token数量_value
Receiving account _value token Numbers increase

支出账户_from减去token数量_value
_from minus the number of token _value spending account

消息发送者可以从账户_from中转出的数量减少_value
The message sender can _from transfer from account to reduce the number of _value

触发转币交易事件
Turn currency trading event trigger

允许_spender从_owner中转出的token数
Allow _spender from _owner transit token number

最多的小数位数
Most of the decimal digits

token简称
Token referred to as"

初始token数量给予消息发送者
The initial token number to give the message sender

```

## 1.1.0 will on the way

## 1.2.0 is in plan
